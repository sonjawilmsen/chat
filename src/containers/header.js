import React from 'react';
import AppBar from '../components/app-bar';

class Header extends React.Component {
  render () {
    return (
      <AppBar title={ this.props.name } style={{ background: }}/>
    );
  }
}

export default Header;
